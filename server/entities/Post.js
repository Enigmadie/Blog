export default class Post {
  constructor(title, content, preview, image, date) {
    this.title = title;
    this.content = content;
    this.preview = preview;
    this.image = image;
    this.date = date;
  }
}
